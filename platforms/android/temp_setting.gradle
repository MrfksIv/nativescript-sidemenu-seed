android {
            defaultConfig {
                applicationId = "org.nativescript.test"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }